apiVersion: v1
kind: ConfigMap
metadata:
  name: dib-common-config
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
