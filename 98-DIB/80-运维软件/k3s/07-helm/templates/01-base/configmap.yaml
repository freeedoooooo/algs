apiVersion: v1
kind: ConfigMap
metadata:
  name: dib-common-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "dib.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
