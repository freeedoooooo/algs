apiVersion: v1
kind: ConfigMap
metadata:
  name: c1-configmap-common
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | replace "NAMESPACE" $.Values.namespace | quote }}
  {{- end }}
