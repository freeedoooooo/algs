# service-rule — 规则引擎
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-rule
  namespace: {{ .Values.namespace }}
  labels:
    app: service-rule
    tier: business
spec:
  replicas: {{ .Values.services.service-rule.replicas }}
  selector:
    matchLabels:
      app: service-rule
  template:
    metadata:
      labels:
        app: service-rule
        tier: business
    spec:
      imagePullSecrets:
        - name: dib-registry-secret
      containers:
        - name: service-rule
          image: {{ .Values.imageRegistry }}/c1/c1-rule:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: 30004
              protocol: TCP
          env:
            - name: TZ
              value: "Asia/Shanghai"
          envFrom:
            - configMapRef:
                name: dib-common-config
          resources:
            {{- toYaml .Values.services.service-rule.resources | nindent 12 }}
          volumeMounts:
            - name: app-logs
              mountPath: /opt/app/logs
            - name: fonts
              mountPath: /usr/share/fonts
              readOnly: true
          livenessProbe:
            httpGet:
              path: /api/rule/actuator/health
              port: 30004
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /api/rule/actuator/health
              port: 30004
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: app-logs
          emptyDir: {}
        - name: fonts
          hostPath:
            path: /usr/share/fonts
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: service-rule
  namespace: {{ .Values.namespace }}
  labels:
    app: service-rule
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 30004
      targetPort: 30004
      protocol: TCP
  selector:
    app: service-rule
