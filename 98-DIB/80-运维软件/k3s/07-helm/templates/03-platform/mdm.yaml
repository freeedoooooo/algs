# mdm — 主数据管理
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mdm
  namespace: {{ .Values.namespace }}
  labels:
    app: mdm
    tier: platform
spec:
  replicas: {{ .Values.services.mdm.replicas }}
  selector:
    matchLabels:
      app: mdm
  template:
    metadata:
      labels:
        app: mdm
        tier: platform
    spec:
      imagePullSecrets:
        - name: dib-registry-secret
      containers:
        - name: mdm
          image: {{ .Values.imageRegistry }}/c1/platform-mdm:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: 20002
              protocol: TCP
          env:
            - name: TZ
              value: "Asia/Shanghai"
          envFrom:
            - configMapRef:
                name: dib-common-config
          resources:
            {{- toYaml .Values.services.mdm.resources | nindent 12 }}
          volumeMounts:
            - name: app-logs
              mountPath: /opt/app/logs
            - name: fonts
              mountPath: /usr/share/fonts
              readOnly: true
          livenessProbe:
            httpGet:
              path: /api/mdm/actuator/health
              port: 20002
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /api/mdm/actuator/health
              port: 20002
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: app-logs
          emptyDir: {}
        - name: fonts
          hostPath:
            path: /usr/share/fonts
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: mdm
  namespace: {{ .Values.namespace }}
  labels:
    app: mdm
spec:
  type: {{ .Values.services.mdm.serviceType }}
  ports:
    - name: http
      port: 20002
      targetPort: 20002
      protocol: TCP
  selector:
    app: mdm
