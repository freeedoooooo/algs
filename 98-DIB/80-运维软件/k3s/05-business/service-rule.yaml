# service-rule — 规则引擎
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-rule
  namespace: c1-idc-test
  labels:
    app: service-rule
    tier: business
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-rule
  template:
    metadata:
      labels:
        app: service-rule
        tier: business
    spec:
      imagePullSecrets:
        - name: dib-registry-secret
      containers:
        - name: service-rule
          image: 10.0.6.183:8088/c1/c1-rule:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 30004
              name: http
              protocol: TCP
          env:
            - name: C1_ENV
              valueFrom:
                configMapKeyRef:
                  name: dib-common-config
                  key: C1_ENV
            - name: TZ
              value: "Asia/Shanghai"
          envFrom:
            - configMapRef:
                name: dib-common-config

          resources:
            requests:
              cpu: "250m"
              memory: "1Gi"
            limits:
              cpu: "1000m"
              memory: "3Gi"
          volumeMounts:
            - name: app-logs
              mountPath: /opt/app/logs
            - name: fonts
              mountPath: /usr/share/fonts
              readOnly: true
          livenessProbe:
            httpGet:
              path: /api/rule/actuator/health
              port: 30004
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /api/rule/actuator/health
              port: 30004
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: app-logs
          emptyDir: {}
        - name: fonts
          hostPath:
            path: /usr/share/fonts
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: service-rule
  namespace: c1-idc-test
  labels:
    app: service-rule
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 30004
      targetPort: 30004
      protocol: TCP
  selector:
    app: service-rule
