apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: dib-c1
  labels:
    app: gateway
spec:
  type: ClusterIP
  ports:
    - port: 20000
      targetPort: 20000
      protocol: TCP
      name: http
  selector:
    app: gateway
