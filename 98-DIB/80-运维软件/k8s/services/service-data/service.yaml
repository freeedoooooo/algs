apiVersion: v1
kind: Service
metadata:
  name: service-data
  namespace: dib-c1
  labels:
    app: service-data
spec:
  type: ClusterIP
  ports:
    - port: 30003
      targetPort: 30003
      protocol: TCP
      name: http
  selector:
    app: service-data
