apiVersion: v1
kind: Service
metadata:
  name: service-extract
  namespace: dib-c1
  labels:
    app: service-extract
spec:
  type: ClusterIP
  ports:
    - port: 30001
      targetPort: 30001
      protocol: TCP
      name: http
  selector:
    app: service-extract
