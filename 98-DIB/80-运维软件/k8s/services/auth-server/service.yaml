apiVersion: v1
kind: Service
metadata:
  name: auth-server
  namespace: dib-c1
  labels:
    app: auth-server
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: http
  selector:
    app: auth-server
