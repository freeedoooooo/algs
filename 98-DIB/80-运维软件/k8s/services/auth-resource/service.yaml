apiVersion: v1
kind: Service
metadata:
  name: auth-resource
  namespace: dib-c1
  labels:
    app: auth-resource
spec:
  type: ClusterIP
  ports:
    - port: 20001
      targetPort: 20001
      protocol: TCP
      name: http
  selector:
    app: auth-resource
