# 可变参数设计原则

[返回章节](README.md) | [返回分类](../README.md) | [返回总目录](../../README.md)

- 状态：已标记完成
- 所属分类：基础巩固
- 所属章节：16 暴力递归到动态规划
- 原始条目：☒ 可变参数设计原则

## 一句话结论
设计递归状态时，可变参数越少越好、类型越简单越好。因为状态参数会直接决定记忆化和 DP 表的规模与复杂度。

## 图片转写 / 题意还原
原图 1：

1. 每一个可变参数的类型，一定不要比 `int` 类型更复杂
2. 原则 1 可以违反，让类型突破到一维线性结构，但必须是唯一可变参数
3. 如果原则 1 被违反，但不违反原则 2，只需要做到记忆化搜索即可
4. 可变参数的个数，能少则少

原图 2：

- 一定要逼自己找到不违反原则情况下的暴力尝试
- 如果找到的暴力尝试不符合原则，马上舍弃，找新的
- 某题如果严重突破这些原则，往往已经非常难了

这篇同样不是单一题目，而是在回答递归 / DP 建模里的一个核心设计问题：

- 当你准备写递归函数时，参数应该怎么选
- 哪些参数是真正会变化、并且会影响答案的状态
- 怎样让这些状态足够简单，方便后续记忆化或写表

换句话说，它的完整“题意”是：

```text
面对一道递归或动态规划题
如何设计尽量少、尽量简单的可变参数
从而让状态空间可控、转表更自然
```

## 解题思路
- 先试着把状态压成少量整数参数
- 如果只能用字符串、数组等结构做状态，优先考虑记忆化搜索而非硬做表
- 只要能重新设计更简单状态，就继续重构递归定义

## 记忆点
- 参数越少越好。
- 类型越简单越好。
- 状态设计决定 DP 难度。
