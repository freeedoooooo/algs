# 技术平台底座架构图

## 1. 汇报范围

本文用于领导汇报场景下说明当前技术平台底座的真实架构情况，重点覆盖以下范围：

- 前端工程：`c1-app`
- 平台基础服务：`data-cloud-auth-server`、`data-cloud-gateway`、`data-cloud-auth-resource`、`data-cloud-mdm`
- 业务域服务：`dib-agent-service-extract`、`dib-agent-service-report`、`dib-agent-service-data`、`dib-agent-service-rule`、`dib-agent-data-dg`
- 公共能力工程：`dib-agent-parent`
- 第三方基础设施：`Nginx`、`Nacos`、`Redis`、`MinIO`、关系型数据库、`Prometheus`、`Grafana`

本文聚焦当前仍在实际运行和持续维护的平台底座与业务域工程。

## 2. 总体判断

当前平台整体采用“前端微前端化 + 后端微服务化 + 公共能力平台化”的技术底座模式：

- 前端侧以 `Nginx` 统一承载静态资源，由 `app-nav` 作为前端平台/壳层承载导航与公共入口，`app-data`、`app-report`、`app-system` 作为依赖其运行的业务应用
- 接入侧由 `data-cloud-gateway` 统一承担 API 路由转发、统一鉴权、OAuth 回调与网关层安全控制
- 后端服务侧按照“平台基础服务在上、业务域服务在下”的结构分层
- 基础设施侧通过 `Nacos + Redis + MinIO + MySQL/DM8/GBase + Prometheus/Grafana` 支撑治理、缓存、文件、数据与监控
- 公共工程侧通过 `dib-agent-parent` 和 starter 体系沉淀通用底座能力

图示约定：

- 实线：依赖 / 调用 / 产出
- 虚线：挂载 / 承载 / 入口

## 3. 总体架构总览

```mermaid
flowchart TB
    user["用户/浏览器"]

    subgraph FE["前端接入层"]
        direction TB
        nginx["Nginx<br/>统一静态资源入口 / API 代理"]
        nav["前端平台<br/>app-nav"]
        bizApps["业务应用<br/>app-data / app-report / app-system"]
    end

    subgraph GW["统一接入层"]
        gateway["data-cloud-gateway<br/>统一路由 / 鉴权 / OAuth 回调"]
    end

    subgraph BACK["后端服务层"]
        direction TB

        subgraph PLATFORM["平台基础服务层"]
            platformSvc["auth-server / auth-resource / mdm<br/>认证、权限、主数据底座"]
        end

        subgraph BIZ["业务域服务层"]
            bizSvc["extract / report / data / rule / dg<br/>资料处理、报告、指标、规则、数据治理"]
        end
    end

    subgraph INFRA["基础设施层"]
        direction TB
        gov["Nacos<br/>注册发现 / 配置管理"]
        dataInfra["Redis / MinIO / MySQL / DM8 / GBase<br/>缓存、文件、关系型数据底座"]
        monitor["Prometheus + Grafana<br/>统一监控"]
    end

    user -. 访问入口 .-> nginx
    nginx -. 静态承载 .-> nav
    bizApps --> nav
    nav --> gateway
    gateway --> platformSvc
    gateway --> bizSvc
    bizSvc --> platformSvc
    platformSvc --> gov
    platformSvc --> dataInfra
    bizSvc --> dataInfra
    bizSvc --> monitor
```

汇报版图（样式增强）：

```mermaid
flowchart TB
    classDef front fill:#EAF3FF,stroke:#2F6BFF,color:#123B5D,stroke-width:1.2px;
    classDef gate fill:#E9FBF8,stroke:#1C8C7A,color:#0F4F46,stroke-width:1.2px;
    classDef platform fill:#EEF8F1,stroke:#2E9E5B,color:#1C5F37,stroke-width:1.2px;
    classDef biz fill:#FFF4EA,stroke:#E8833A,color:#8A4B1F,stroke-width:1.2px;
    classDef infra fill:#F4F7FB,stroke:#7B8A9A,color:#32404D,stroke-width:1.2px;

    user2["👤 用户 / 浏览器"]:::front

    subgraph FE2["前端接入层"]
        direction TB
        nginx2["🌐 Nginx<br/>静态资源入口 / API 代理"]:::front
        nav2["🧭 前端平台<br/>app-nav"]:::front
        bizApps2["🖥 业务应用<br/>app-data / app-report / app-system"]:::front
    end

    subgraph GW2["统一接入层"]
        gateway2["🧭 data-cloud-gateway<br/>统一路由 / 鉴权 / OAuth 回调"]:::gate
    end

    subgraph BACK2["后端服务层"]
        direction TB
        platformSvc2["🛡 平台基础服务<br/>auth-server / auth-resource / mdm"]:::platform
        bizSvc2["⚙ 业务域服务<br/>extract / report / data / rule / dg"]:::biz
    end

    subgraph INFRA2["基础设施层"]
        direction TB
        nacos2["📍 Nacos<br/>注册发现 / 配置管理"]:::infra
        dataInfra2["🗄 Redis / MinIO / MySQL / DM8 / GBase<br/>缓存 / 文件 / 数据底座"]:::infra
        monitor2["📊 Prometheus + Grafana<br/>统一监控"]:::infra
    end

    style FE2 fill:#F7FBFF,stroke:#B9D5FF,stroke-width:1px,color:#123B5D
    style GW2 fill:#F2FFFC,stroke:#A8E3D8,stroke-width:1px,color:#0F4F46
    style BACK2 fill:#FFFDF8,stroke:#F1D9BC,stroke-width:1px,color:#5A432A
    style INFRA2 fill:#F8FAFC,stroke:#D6E0EA,stroke-width:1px,color:#32404D

    user2 -. 访问入口 .-> nginx2
    nginx2 -. 静态承载 .-> nav2
    bizApps2 --> nav2
    nav2 --> gateway2
    gateway2 --> platformSvc2
    gateway2 --> bizSvc2
    bizSvc2 --> platformSvc2
    platformSvc2 --> nacos2
    platformSvc2 --> dataInfra2
    bizSvc2 --> dataInfra2
    bizSvc2 --> monitor2
```

## 4. 部署架构视图

```mermaid
flowchart TB
    browser["浏览器/终端访问"]

    subgraph ACCESS["访问入口区"]
        direction TB
        nginx["Nginx 容器<br/>80/8090<br/>承载前端静态资源并代理 API"]
        frontendPkg["前端静态资源目录<br/>/opt/app/c1-client"]
    end

    subgraph APPNET["应用服务区（Docker 网络 c1-network）"]
        direction TB
        gateway["platform-gateway<br/>20000"]
        platformSvc["平台基础服务组<br/>auth-server / auth-resource / mdm"]
        bizSvc["业务服务组<br/>extract / report / data / rule / dg"]
    end

    subgraph MIDDLEWARE["中间件与基础设施区"]
        direction TB
        nacos["Nacos 2.3.2<br/>注册发现 / 配置"]
        storage["Redis 7.0 / MinIO<br/>缓存、会话、文件对象存储"]
        db["MySQL / DM8 / GBase<br/>关系型数据底座"]
        monitor["Prometheus + Grafana<br/>统一监控"]
    end

    subgraph EXTERNAL["外部能力接口"]
        direction TB
        ocr["OCR 服务"]
        ftp["FTP / 外部数据源"]
    end

    browser -. 访问入口 .-> nginx
    frontendPkg -. 静态资源挂载 .-> nginx
    nginx --> gateway
    gateway --> platformSvc
    gateway --> bizSvc
    bizSvc --> platformSvc
    gateway --> nacos
    platformSvc --> storage
    platformSvc --> db
    bizSvc --> storage
    bizSvc --> db
    bizSvc --> monitor
    bizSvc --> EXTERNAL
```

汇报版图（样式增强）：

```mermaid
flowchart TB
    classDef front fill:#EAF3FF,stroke:#2F6BFF,color:#123B5D,stroke-width:1.2px;
    classDef gate fill:#E9FBF8,stroke:#1C8C7A,color:#0F4F46,stroke-width:1.2px;
    classDef service fill:#FFF4EA,stroke:#E8833A,color:#8A4B1F,stroke-width:1.2px;
    classDef infra fill:#F4F7FB,stroke:#7B8A9A,color:#32404D,stroke-width:1.2px;
    classDef ext fill:#FFF1F1,stroke:#C94F4F,color:#7C2E2E,stroke-width:1.2px;

    browser2["👤 浏览器 / 终端"]:::front

    subgraph ACCESS2["访问入口区"]
        direction TB
        nginx2["🌐 Nginx<br/>80 / 8090"]:::front
        pkg2["📦 /opt/app/c1-client<br/>前端静态资源"]:::front
    end

    subgraph APP2["应用服务区"]
        direction TB
        gateway2["🧭 platform-gateway<br/>20000"]:::gate
        platform2["🛡 平台基础服务组<br/>auth-server / auth-resource / mdm"]:::service
        biz2["⚙ 业务服务组<br/>extract / report / data / rule / dg"]:::service
    end

    subgraph INFRA2["中间件与基础设施区"]
        direction TB
        nacos2["📍 Nacos 2.3.2"]:::infra
        storage2["🗂 Redis 7.0 / MinIO"]:::infra
        db2["🗄 MySQL / DM8 / GBase"]:::infra
        monitor2["📊 Prometheus + Grafana"]:::infra
    end

    subgraph EXT2["外部能力接口"]
        direction TB
        ocr2["🔎 OCR 服务"]:::ext
        ftp2["🔗 FTP / 外部数据源"]:::ext
    end

    style ACCESS2 fill:#F7FBFF,stroke:#B9D5FF,stroke-width:1px,color:#123B5D
    style APP2 fill:#FFFDF8,stroke:#F1D9BC,stroke-width:1px,color:#5A432A
    style INFRA2 fill:#F8FAFC,stroke:#D6E0EA,stroke-width:1px,color:#32404D
    style EXT2 fill:#FFF8F8,stroke:#F0C2C2,stroke-width:1px,color:#7C2E2E

    browser2 -. 访问入口 .-> nginx2
    pkg2 -. 挂载 .-> nginx2
    nginx2 --> gateway2
    gateway2 --> platform2
    gateway2 --> biz2
    biz2 --> platform2
    gateway2 --> nacos2
    platform2 --> storage2
    platform2 --> db2
    biz2 --> storage2
    biz2 --> db2
    biz2 --> monitor2
    biz2 --> ocr2
    biz2 --> ftp2
```

## 5. 模块结构视图

说明：以下箭头统一表示“依赖方向”，即业务层依赖平台层，平台层依赖公共能力层。

```mermaid
flowchart TB
    subgraph FRONT["微前端模块"]
        direction TB
        bizApp["业务层<br/>app-data / app-report / app-system"]
        navApp["平台层<br/>app-nav"]
        shared["共享层<br/>utils / components / composables / config"]
    end

    subgraph BACKEND["后端模块"]
        direction TB

        subgraph DOMAIN["业务域模块"]
            direction TB
            domainSvc["extract / report / data / rule / dg"]
        end

        subgraph PLATFORM["平台基础模块"]
            direction TB
            platformBase["gateway / auth / resource / mdm"]
        end

        subgraph COMMON["公共能力模块"]
            direction TB
            commonBase["parent / dependencies / starter"]
        end
    end

    bizApp --> navApp
    bizApp --> shared
    navApp --> shared
    domainSvc --> platformBase
    domainSvc --> commonBase
    platformBase --> commonBase
```

汇报版图（样式增强）：

```mermaid
flowchart TB
    classDef front fill:#EAF3FF,stroke:#2F6BFF,color:#123B5D,stroke-width:1.2px;
    classDef common fill:#F5F1FF,stroke:#8C67D9,color:#4D347A,stroke-width:1.2px;
    classDef platform fill:#EEF8F1,stroke:#2E9E5B,color:#1C5F37,stroke-width:1.2px;
    classDef biz fill:#FFF4EA,stroke:#E8833A,color:#8A4B1F,stroke-width:1.2px;

    subgraph FRONT2["微前端模块"]
        direction TB
        bizApp2["🖥 业务层<br/>app-data / app-report / app-system"]:::front
        navApp2["🧭 平台层<br/>app-nav"]:::front
        shared2["🧩 共享层<br/>utils / components / composables / config"]:::front
    end

    subgraph BACKEND2["后端模块"]
        direction TB
        domain2["⚙ 业务域模块<br/>extract / report / data / rule / dg"]:::biz
        platform2["🛡 平台基础模块<br/>gateway / auth / resource / mdm"]:::platform
        common2["🧱 公共能力模块<br/>parent / dependencies / starter"]:::common
    end

    style FRONT2 fill:#F7FBFF,stroke:#B9D5FF,stroke-width:1px,color:#123B5D
    style BACKEND2 fill:#FFFDF8,stroke:#F1D9BC,stroke-width:1px,color:#5A432A

    bizApp2 --> navApp2
    bizApp2 --> shared2
    navApp2 --> shared2
    domain2 --> platform2
    domain2 --> common2
    platform2 --> common2
```

## 6. 核心业务处理链路

```mermaid
flowchart LR
    subgraph ACCESS["业务入口"]
        direction TB
        appNav["app-nav"]
        appData["app-data"]
        appReport["app-report"]
        appSystem["app-system"]
    end

    gateway["gateway"]

    subgraph INPUT["输入与治理数据"]
        direction TB
        files["业务文件"]
        govData["治理数据表"]
    end

    subgraph SERVICE["核心处理服务"]
        direction LR
        extract["extract<br/>文件提取"]
        dataSvc["data<br/>指标/规则计算"]
        frameSvc["rule（frame）<br/>框架计算"]
        reportSvc["report<br/>报告生成"]
    end

    subgraph OUTPUT["业务产出"]
        direction TB
        calcResult["计算结果<br/>指标 / 规则 / 框架"]
        reportFile["报告文件"]
    end

    appData --> appNav
    appReport --> appNav
    appSystem --> appNav
    appNav --> gateway

    gateway --> extract
    gateway --> dataSvc
    gateway --> frameSvc
    gateway --> reportSvc

    files --> extract
    extract --> dataSvc
    govData --> dataSvc
    dataSvc --> frameSvc
    govData --> reportSvc
    dataSvc --> reportSvc
    frameSvc --> reportSvc
    dataSvc --> calcResult
    frameSvc --> calcResult
    reportSvc --> reportFile
```

汇报版图（样式增强）：

```mermaid
flowchart LR
    classDef front fill:#EAF3FF,stroke:#2F6BFF,color:#123B5D,stroke-width:1.2px;
    classDef input fill:#FFF8EE,stroke:#E8833A,color:#8A4B1F,stroke-width:1.2px;
    classDef service fill:#EEF8F1,stroke:#2E9E5B,color:#1C5F37,stroke-width:1.2px;
    classDef output fill:#E9FBF8,stroke:#1C8C7A,color:#0F4F46,stroke-width:1.2px;

    subgraph ACCESS2["业务入口"]
        direction TB
        appNav2["🧭 app-nav"]:::front
        appData2["🖥 app-data"]:::front
        appReport2["🖥 app-report"]:::front
        appSystem2["🖥 app-system"]:::front
    end

    gateway2["🧭 gateway"]:::front

    subgraph INPUT2["输入与治理数据"]
        direction TB
        files2["📄 业务文件"]:::input
        govData2["🗂 治理数据表"]:::input
    end

    extract2["🔎 extract<br/>文件提取"]:::service
    data2["📐 data<br/>指标 / 规则计算"]:::service
    frame2["🧮 rule（frame）<br/>框架计算"]:::service
    report2["📝 report<br/>报告生成"]:::service

    subgraph OUTPUT2["业务产出"]
        direction TB
        calc2["📊 计算结果<br/>指标 / 规则 / 框架"]:::output
        file2["📘 报告文件"]:::output
    end

    style ACCESS2 fill:#F7FBFF,stroke:#B9D5FF,stroke-width:1px,color:#123B5D
    style INPUT2 fill:#FFFDF8,stroke:#F1D9BC,stroke-width:1px,color:#8A4B1F
    style OUTPUT2 fill:#F2FFFC,stroke:#A8E3D8,stroke-width:1px,color:#0F4F46

    appData2 --> appNav2
    appReport2 --> appNav2
    appSystem2 --> appNav2
    appNav2 --> gateway2 --> extract2 --> data2 --> frame2 --> report2 --> file2
    files2 --> extract2
    govData2 --> data2
    govData2 --> report2
    data2 --> calc2
    frame2 --> calc2
    data2 --> report2
    frame2 --> report2
```

说明：

- `extract` 服务负责从业务文件中提取结构化数据，形成后续计算输入
- `data` 服务基于提取数据和治理数据表进行指标计算与规则计算
- `frame` 当前在工程实现上由 `rule` 服务承载，依据 `data` 服务输出的规则结果完成框架计算
- `report` 服务综合治理数据表、指标结果、规则结果和框架结果生成报告

## 7. 分层职责说明

### 7.1 前端接入层

- `Nginx` 统一承载前端静态资源，并承担前后端分离场景下的 API 反向代理
- `c1-app` 以前端平台 + 业务应用的方式组织功能，`app-nav` 作为统一导航、壳层与公共入口平台，`app-data`、`app-report`、`app-system` 在其之上承载各业务子域
- 通过 `shared-utils`、`shared-components`、`shared-composables`、`shared-config` 复用跨应用能力，降低重复开发成本

### 7.2 网关接入层

- `data-cloud-gateway` 是统一接入入口
- 对外负责 API 路由、安全控制、认证过滤、统一入口治理
- 对认证场景负责 `/api-oauth` 回调与令牌交换

### 7.3 公共能力层

- `dib-agent-parent` 统一管理业务服务的父工程与版本基线
- `data-cloud-dependencies` 统一 Spring、数据库驱动、ORM、工具库等依赖版本
- `starter` 体系沉淀缓存、数据源、文件、Web、任务、AI 等通用基础能力

### 7.4 平台基础服务层

- `data-cloud-auth-server`：统一认证、登录、令牌签发、OAuth2 流程
- `data-cloud-auth-resource`：统一用户、角色、模块、接口权限、密级等权限资源管理
- `data-cloud-mdm`：统一组织、项目、字典、附件等主数据能力

### 7.5 业务域服务层

- `extract`：资料提取、ETL、清单管理、文档定位
- `report`：报告生成、模板管理、变量计算、文档渲染
- `data`：指标计算、元数据、数据资源管理
- `rule`：规则框架、业务规则、AI 规则计算
- `dg`：外部数据同步与数据治理

说明：

- `dib-agent-data-dg` 当前在部署层已独立存在，但在统一对外网关入口中未见与 `extract/report/data/rule` 同级的对外路由暴露，汇报时建议表述为“内部治理类服务”

## 8. 国产化兼容情况

当前技术底座已具备较明确的国产化兼容能力基础：

- 数据库层已不仅限于 `MySQL`
- `auth-server` 明确具备 `MySQL + DM8` 兼容能力
- `auth-resource`、`mdm` 工程中已纳入 `DM8` 驱动与 `GBase` 驱动
- 公共依赖工程中已纳入 `anyline-data-jdbc-gbase`、`Dm8JdbcDriver18` 等兼容依赖
- 部分项目部署文档已出现 `DM8 / GBase` 的实际接入说明，说明平台已在不同项目环境中落地国产数据库适配

建议汇报口径：

- 平台当前以关系型数据库为核心数据底座，主流部署支持 `MySQL`
- 在国产化场景下，已具备向 `DM8 / GBase` 迁移与适配的技术基础，部分项目已完成相关接入

## 9. 架构特点与关注点

### 9.1 主要特点

- 分层清晰，平台能力与业务能力边界相对明确
- 统一接入、统一鉴权、统一主数据，底座复用度高
- 公共 starter 沉淀较完整，业务服务扩展效率较高
- 已具备信创数据库兼容基础
- 部署、监控、中间件链路相对完整

### 9.2 当前关注点

- 前端仍存在历史栈与新栈并存的演进痕迹，本次汇报未展开旧栈差异
- 后端 Spring Boot 版本口径存在 `2.5.x` 与业务版本号 `2.7.x` 并存的现象，后续建议进一步收敛
- `DG` 服务当前更偏内部治理能力，对外入口治理口径需要继续统一

## 10. 演进建议

- 继续收敛前端应用技术基线，统一到同一代 Vue 3 + Vite 体系
- 继续收敛后端父工程与依赖管理版本，降低平台长期维护复杂度
- 将内部治理类服务的对外暴露策略、网关接入策略形成统一标准
- 对 `Nacos`、监控、日志、对象存储、数据库适配形成更明确的平台级标准模板
- 在信创场景下继续固化 `MySQL -> DM8 / GBase` 的迁移规范与验证清单
